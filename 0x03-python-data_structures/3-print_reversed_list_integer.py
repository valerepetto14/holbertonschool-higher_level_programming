#!/usr/bin/python3
def print_reversed_list_integer(my_list=[]):
    lentlist = len(my_list)
    lentlist -= 1
    lista = []
    if (type(lista) == type(my_list)):
            my_list.reverse()
            for i in my_list
                print(f"{i:d}")
